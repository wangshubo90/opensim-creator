#pragma once

namespace osc
{
    enum class SimulationUIPlaybackState {
        Playing,
        Stopped,
        NUM_OPTIONS
    };

    enum class SimulationUIRecorderState {
        Recording,
        Stopped,
        NUM_OPTIONS
    };
}
